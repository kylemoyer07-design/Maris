import PlaceholderPage from "@/components/PlaceholderPage";

export default function ImagesPage() {
  return (
    <PlaceholderPage
      title="Device Images"
      blurb="Symbol PDFs and photos linked to part numbers — the symbols that get placed on the drawing."
    />
  );
}
