import PlaceholderPage from "@/components/PlaceholderPage";

export default function DatasheetsPage() {
  return (
    <PlaceholderPage
      title="Datasheets & Manuals"
      blurb="One datasheet per part number, grouped by brand. Upload once and every OP that pulls the part links straight to it."
    />
  );
}
