import PlaceholderPage from "@/components/PlaceholderPage";

export default function CadPage() {
  return (
    <PlaceholderPage
      title="CAD Files"
      blurb="One CAD block per part number, grouped by brand. Missing blocks are flagged so you can see what still needs drawing."
    />
  );
}
